# relative distance threshold for simplifying time series for preprocessing
const SIMPLIFY_THRESHOLD = 0.005

# threshold for relative improvement in k-means clustering with refinement
const CLUSTERING_THRESHOLD = 0.01

# absolute distance threshold for splitting time series into pieces (RDP algorithm)
const SPLIT_DISTANCE_RDP = 1.0

# absolute distance threshold for alternative splitting time series into pieces
const SPLIT_DISTANCE_ALT = 1.0

# maximal time step in simulation
const DTMAX = 0.2

# radius of tube around a simulation
const TUBE_RADIUS = 0.1

# --- unused ---

# time step for box reachability algorithm (alternative simulation)
const TIME_STEP_BOX_REACH = 0.01
